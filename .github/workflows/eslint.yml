name: eslint

on: 
  push:
    branches:
      - main
    workflow_dispatch:

jobs:
  eslint:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: check out repo
        uses: actions/checkout@v2

      - name: set up node
        uses: actions/setup-node@v3
        with:
            node-version: '18.x'

      - name: npm install
        run: npm install

      - name: eslint
        run: npm run lint